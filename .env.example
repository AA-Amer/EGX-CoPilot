# PRIMARY — free key at console.groq.com
GROQ_API_KEY=your_groq_key_here

# BACKUP — free at aistudio.google.com
GEMINI_API_KEY=your_gemini_key_here

# FUTURE — uncomment when ready to activate
# ANTHROPIC_API_KEY=your_claude_key_here

APP_ENV=development

# SQLite database path — create the directory first if it doesn't exist
DB_PATH=D:/egx_copilot.db
